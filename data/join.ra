LOAD STUDENTS
LOAD COURSES
A <- JOIN STUDENTS, COURSES ON ID <= ROLLNO
B <- JOIN STUDENTS, COURSES ON ID < ROLLNO
C <- JOIN STUDENTS, COURSES ON ID >= ROLLNO
D <- JOIN STUDENTS, COURSES ON ID > ROLLNO
E <- JOIN STUDENTS, COURSES ON ID == ROLLNO